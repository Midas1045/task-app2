apiVersion: v1
kind: Service
metadata:
  name: my-nodeport-svc
  namespace: bank-app

spec:
  type: "NodePort"
  selector:
    app: my-app

  ports:
  - protocol: TCP
    port: 1234
    targetPort: 1234

